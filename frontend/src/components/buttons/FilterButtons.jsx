/** Styled component to wrap around components to style */
import Wrapper from "../../assets/wrappers/FilterButtonWrapper.js";

function FilterButtons({ label }) {
  return <Wrapper>{label}</Wrapper>;
}
export default FilterButtons;
