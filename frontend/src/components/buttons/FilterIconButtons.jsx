/** import the styled component wrapper to apply styles */
import Wrapper from "../../assets/wrappers/FilterIconButtonWrapper.js";
function FilterIconButtons({ label }) {
  return <Wrapper>{label}</Wrapper>;
}
export default FilterIconButtons;
